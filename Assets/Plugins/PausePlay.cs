using UnityEngine;

public class PausePlay : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    public void PausePlayeGame()
    { 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
